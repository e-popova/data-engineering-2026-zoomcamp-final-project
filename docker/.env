GOOGLE_APPLICATION_CREDENTIALS=/keys/sa_terraform_key.json # change file name if needed
GOOGLE_PROJECT_ID=de-zoomcamp2026-final # change project id
GOOGLE_LOCATION=EU
GCS_BUCKET=gs://crypto-data-full # change bucket name if needed (without / at the end)
PARTITION_INTERVAL=600 # this is partitioning in GCS in seconds.
CHECKPOINT_INTERVAL=300 # checkpoint interval in seconds